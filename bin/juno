#!/bin/bash
DIR="$(dirname $(readlink -f "$0"))"
pushd "$DIR"/../scripts > /dev/null
    . activate-juliawin-environment.sh
popd > /dev/null


#for some reason juno hates seeing julia in the current path
[[ -f julia || -f julia.exe ]] && cd ~

"$juliawin_vendor\atom\atom" "$@"

exit $?